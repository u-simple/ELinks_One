name: Trigger auto deployment for elinksdev

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ dev ]
    paths:
    - '**'
    - '.github/workflows/elinksdev-AutoDeployTrigger-098fbe90-99f2-4f55-b0b7-1a39d21dd5ec.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.ELINKSDEV_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.ELINKSDEV_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.ELINKSDEV_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: 
          registryUsername: ${{ secrets.ELINKSDEV_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.ELINKSDEV_REGISTRY_PASSWORD }}
          containerAppName: elinksdev
          resourceGroup: DefaultResourceGroup-CCAN
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


